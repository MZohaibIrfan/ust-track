import { Placeholder } from "../components/Placeholder";

export function OverviewPage() {
  return (
    <Placeholder
      title="UST Track"
      body="Degree and timetable planning for HKUST undergraduates. Timetable, Degree, and Career each get their own advisor agent — Overview stays a plain dashboard."
    />
  );
}
