import { Placeholder } from "../components/Placeholder";

export function CareerPage() {
  return (
    <Placeholder
      accent="var(--page-career)"
      eyebrow="Building next"
      title="Career"
      body="Co-op and internship tracking — applications, deadlines, and terms taken off for work. A career agent will line these up against your degree plan and timetable so nothing clashes."
    />
  );
}
