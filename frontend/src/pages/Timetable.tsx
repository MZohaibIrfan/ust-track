import { Placeholder } from "../components/Placeholder";

export function TimetablePage() {
  return (
    <Placeholder
      title="Timetable"
      body="SIS paste, catalog search, weekly grid, overlap flags, enrolled list, and .ics export will live here. Selections persist on the server by planner_id."
    />
  );
}
